{{>header}}
{{>navbar}}
<section class="container my-3">
{{#if post.title}}
    <h2>{{post.title}}</h2>
    <p>{{post.body}}</p>
{{else}}
<div class="alert alert-danger">
    invalid post id
</div>
{{/if}}
</section>
{{>footer}}